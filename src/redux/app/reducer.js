import * as types from "./actionTypes";

const initialState ={
    choosedCam : "user",
    photos:[],
};

const appReducer = (state=initialState,{type,payload})=>{
    switch (type) {
        case value:
            
            break;
    
        default:
            return state;
    }
}